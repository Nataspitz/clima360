import styled from "styled-components";
import background from "../assets/bg_image.webp"

export const BackgroundImage = styled.div`
    background-image: url(${background});
    width: 100%;
    height: 100vh;
`